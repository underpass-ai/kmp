"""Whole-unit token meter over retained KMP journey captures (#544, increment I0).

Counts are reference counts for a declared encoder and representation. They are
not billing, not host context and never summed across representations.
"""
