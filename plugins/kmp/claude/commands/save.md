---
description: Checkpoint this project's maintained KMP bundle and show its diff
argument-hint: "[no arguments]"
---

Use the `kmp-save` skill. It owns export repair, diff review and the boundary
that forbids staging the bundle unless the user asks.
