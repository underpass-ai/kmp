---
description: Restore project memory from its committed KMP bundle
argument-hint: "[no arguments]"
---

Use the `kmp-restore` skill. It owns the empty-store precondition and must not
delete or overwrite an existing memory to make restore succeed.
