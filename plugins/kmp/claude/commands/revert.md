---
description: Supersede a KMP decision without deleting history
argument-hint: "[what to revert]"
---

Use the `kmp-revert` skill for `$ARGUMENTS`. It owns target confirmation,
the compensating write and the before/after proof.
