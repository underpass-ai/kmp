---
description: Show the installed KMP version, selected store, tools and viewer
argument-hint: "[no arguments]"
---

Use the `kmp-info` skill. It is the single source of truth for this identity
report; this command adds no diagnosis of its own.
