---
description: Catch up on an about from KMP's temporal event history
argument-hint: "[about] [since]"
---

Use the `kmp-catchup` skill for `$ARGUMENTS`. The skill is the single source
of truth for temporal boundaries, pagination, audit and reporting.
