# underpass-ai/kmp PR #59: chore: v0.1.6

## Integration record

- **Status:** merged
- **Pull request:** [#59](https://github.com/underpass-ai/kmp/pull/59)
- **Author:** [`tgarciai`](https://github.com/tgarciai)
- **Created:** `2026-08-17T07:28:59Z`
- **Merged:** `2026-08-17T07:37:01Z`
- **Base:** `main` at `1fc820b61f3c9ddd28d32c9b7cfa982c30f66014`
- **Head:** `chore/v0.1.6` at `f54d24bc0614e285f181ea292963c901f9a8f61d`
- **Merge commit:** [`15a543e7b6622cbe7612d9af76c3cf6bfd60eecb`](https://github.com/underpass-ai/kmp/commit/15a543e7b6622cbe7612d9af76c3cf6bfd60eecb)
- **Integration method:** single-parent integration (squash or rebase)
- **Audited tree range:** `1fc820b61f3c9ddd28d32c9b7cfa982c30f66014..15a543e7b6622cbe7612d9af76c3cf6bfd60eecb`

## Normalized impact

- **Classification:** `release`
- **Capability added:** No new capability is explicitly claimed by the PR title.
- **Behavior or maintenance changed:** chore: v0.1.6
- **Capability removed:** No capability removal is explicitly claimed by the title or integrated commit subjects.

This classification is deliberately conservative. The title and integrated
commit subjects support the normalized claims; the complete PR description and
tree evidence below remain the authority for detailed behavior.

## Author's change description

Twelve fixes found by driving the product as a user rather than as its author.

## What is in it

**The viewer** — the Timeline landed blank and Replay claimed there was nothing to replay (#39); the time column printed the store's sort key and `SNAPSHOT pending` never resolved (#41); the budget moved the numbers but never the picture (#40); `depth=abc` answered 200 while `scope` beside it refused, and `HEAD` answered 405 (#42); and Replay ended with two thirds of the graph dark (#57).

**The memory surface** — the first write to a fresh about was impossible (#14); wake now hands back the `resume_cursor` it already had (#25); a replaced entry comes back marked, in a channel deliberately separate from `conflicts` (#28); and dimension scoping is documented as what it is *for* (#33).

**The operator's paths** — `share-memory` turns seven manual steps into one command and the doctor offers it (#43); the doctor reports plugin/binary version drift (#44); a server that dies at startup now leaves a trace, and the doctor reads it (#45).

**CI** — merging to main no longer re-runs the gates on a tree already proved green (#31), and a test that raced the projection now waits for it (#30, partly).

## The CHANGELOG was empty

Across all twelve pull requests, `Unreleased` stayed `## [Unreleased]` and nothing else — the same defect this file's own contract exists to prevent, and the same one MADE had yesterday. Written now, grouped by what a reader would look for rather than by pull request order.

## Checklist

| gate | result |
|---|---|
| `quality-gate.sh` | pass, 811 tests |
| `helm-lint.sh` | pass |
| `check-publish-chain.sh`, `check-vendored-contract.sh` | pass |
| `embedded-sqlite-gates.sh` | pass, including `share-memory` and the two-host launcher check |
| `kmp-plugin-install-smoke.sh`, `kmp-plugin-smoke.sh` | pass |
| `embedded-binary-gates.sh` | pass |

## One operational note

The disk hit **49MB free of 916GB** mid-release — 126GB in `kmp/target`, 70GB in `made/target` — and the first gate run failed on artifacts written while it was full. That is not a code failure and the results above are from a clean re-run.

`cargo-sweep-ai`, set up this morning for exactly this, reclaimed **210GB** when run by hand instead of waiting for its Sunday timer. Worth knowing that a weekly cadence is not enough for two workspaces this size.

## Verified repository evidence

- **Integrated commits:** 1
- **Changed files:** 6
- **Text additions/deletions:** +134 / -42
- **Binary files:** 0
- **Top-level areas:** `plugins`, `CHANGELOG.md`, `Cargo.lock`, `Cargo.toml`, `charts`

### Integrated commits

- [`15a543e7b662`](https://github.com/underpass-ai/kmp/commit/15a543e7b6622cbe7612d9af76c3cf6bfd60eecb) chore: v0.1.6

<details>
<summary>Changed paths (6)</summary>

- `M` `CHANGELOG.md` (+93 / -1)
- `M` `Cargo.lock` (+24 / -24)
- `M` `Cargo.toml` (+13 / -13)
- `M` `charts/kmp/Chart.yaml` (+2 / -2)
- `M` `plugins/kmp/.claude-plugin/plugin.json` (+1 / -1)
- `M` `plugins/kmp/.codex-plugin/plugin.json` (+1 / -1)

</details>

## Audit note

The repository diff above is calculated from the first parent of the recorded
merge commit to the merge commit itself. It therefore records what this PR
introduced into `main`, even when the branch was stale or GitHub used a squash
merge. Statements in the PR description are author claims; changed paths and
commits are repository evidence.
