# underpass-ai/rehydration-kernel PR #147: feat: publish the consumer memory contract as rehydration-memory-api

## Integration record

- **Status:** merged
- **Pull request:** [#147](https://github.com/underpass-ai/rehydration-kernel/pull/147)
- **Author:** [`tgarciai`](https://github.com/tgarciai)
- **Created:** `2026-08-04T17:12:17Z`
- **Merged:** `2026-08-04T17:23:58Z`
- **Base:** `main` at `e03286b144efb6b1a66ba0a5e8f468f80db84472`
- **Head:** `feat/consumer-memory-api` at `e7c6ddbe75c48b7ab7f51b18c81967f855309d37`
- **Merge commit:** [`97c04189fc8a1b50d2d318d6033dd24745b02dc4`](https://github.com/underpass-ai/rehydration-kernel/commit/97c04189fc8a1b50d2d318d6033dd24745b02dc4)
- **Integration method:** single-parent integration (squash or rebase)
- **Audited tree range:** `e03286b144efb6b1a66ba0a5e8f468f80db84472..97c04189fc8a1b50d2d318d6033dd24745b02dc4`

## Normalized impact

- **Classification:** `feature`
- **Capability added:** feat: publish the consumer memory contract as rehydration-memory-api
- **Behavior or maintenance changed:** The PR is classified as a new capability; inspect the evidence below for supporting changes.
- **Capability removed:** No capability removal is explicitly claimed by the title or integrated commit subjects.

This classification is deliberately conservative. The title and integrated
commit subjects support the normalized claims; the complete PR description and
tree evidence below remain the authority for detailed behavior.

## Author's change description

Products that embed the kernel have been importing `rehydration-embedded` and, with it, the domain: `rehydration-domain` aggregates and `rehydration-application` queries crossing into a consumer's dependency graph, where every internal change of ours is a possible break of theirs. The kernel promised its consumers stability — but there was no artifact whose version the promise attached to.

## The crate

`rehydration-memory-api` is a **leaf crate** — no kernel dependency — holding what an embedding product is allowed to know. It is the sibling of `rehydration-plugin-api`, pointing the other way: that crate is what a plugin may know about the kernel, this one is what an embedding product may know.

| | |
|---|---|
| `MemoryRecallView` + parts | plain projections; the rendered context travels with its content hash, so a consumer can verify a model received exactly what the kernel rendered |
| `ApiCapabilities` | stated by the implementation, checked at startup — two builds of one release can differ in features, and a version string cannot say so |
| `ApiError` | publishes `is_transient()` per failure, so consumers do not keep retry tables that go stale when the enum grows |
| `MemoryRecallApi` | `wake` and `ask`, reads only |

`CONTRACT_VERSION` moves on **meaning**, not on release. The recall tiers are named in the contract (`Summary`, `CausalSpine`, `EvidencePack`) without exporting the domain's resolution ladder.

**Mutations are deliberately absent from v1.** Ingestion, projection and export have their own commands, idempotency and provenance inside the kernel; the contract grows by adding named capabilities, never by widening existing ones.

**The answer policy default stays the strict one**, `EvidenceOrUnknown`: a memory that answers beyond its evidence is worse than one that says it does not know, and a consumer that wants a looser policy asks for it by name.

## The implementation

`rehydration-embedded` implements the trait for `EmbeddedKernel`; the dependency arrow points from implementation to contract, never back. Trait methods return named `Send` futures rather than `async fn`, so the trait is consumable from generic code on multi-threaded runtimes **without adding an `async-trait` dependency this workspace does not otherwise carry**.

## Tests

Ten new: six on the contract types, four driving the embedded kernel **through the trait alone**. The test file states its own rule: a domain type may arrange the scene but never appear in an assertion — that is the leak the crate exists to prevent. Among them: an unknown about must never read as a healthy empty memory.

[ADR-015](https://github.com/underpass-ai/rehydration-kernel/blob/97c04189fc8a1b50d2d318d6033dd24745b02dc4/docs/adr/ADR-015-consumer-memory-api-contract.md) records the decision and what stays deliberately outside the contract.

## Local gate

`cargo test --workspace` 743 passing · `cargo fmt --check` clean · `cargo clippy --workspace --all-targets -D warnings` clean · pre-commit coverage gate passed at 87.72% line coverage.

## Downstream

The known first consumer can bind its own memory port to this trait and stop importing kernel domain types; its existing deep adapter keeps working unchanged until it migrates. Same pattern as the coordination engine's `choreo-api`, merged earlier today.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Verified repository evidence

- **Integrated commits:** 1
- **Changed files:** 15
- **Text additions/deletions:** +827 / -0
- **Binary files:** 0
- **Top-level areas:** `crates`, `docs`, `Cargo.lock`, `Cargo.toml`

### Integrated commits

- [`97c04189fc8a`](https://github.com/underpass-ai/rehydration-kernel/commit/97c04189fc8a1b50d2d318d6033dd24745b02dc4) feat: publish the consumer memory contract as rehydration-memory-api (#147)

<details>
<summary>Changed paths (15)</summary>

- `M` `Cargo.lock` (+9 / -0)
- `M` `Cargo.toml` (+2 / -0)
- `M` `crates/rehydration-embedded/Cargo.toml` (+1 / -0)
- `M` `crates/rehydration-embedded/src/lib.rs` (+1 / -0)
- `A` `crates/rehydration-embedded/src/memory_api.rs` (+149 / -0)
- `A` `crates/rehydration-embedded/tests/memory_api_contract.rs` (+166 / -0)
- `A` `crates/rehydration-memory-api/Cargo.toml` (+15 / -0)
- `A` `crates/rehydration-memory-api/src/api_capabilities.rs` (+73 / -0)
- `A` `crates/rehydration-memory-api/src/api_error.rs` (+67 / -0)
- `A` `crates/rehydration-memory-api/src/lib.rs` (+38 / -0)
- `A` `crates/rehydration-memory-api/src/memory_recall_api.rs` (+33 / -0)
- `A` `crates/rehydration-memory-api/src/memory_requests.rs` (+98 / -0)
- `A` `crates/rehydration-memory-api/src/memory_views.rs` (+107 / -0)
- `A` `docs/adr/ADR-015-consumer-memory-api-contract.md` (+67 / -0)
- `M` `docs/adr/README.md` (+1 / -0)

</details>

## Audit note

The repository diff above is calculated from the first parent of the recorded
merge commit to the merge commit itself. It therefore records what this PR
introduced into `main`, even when the branch was stale or GitHub used a squash
merge. Statements in the PR description are author claims; changed paths and
commits are repository evidence.
