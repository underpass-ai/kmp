# underpass-ai/rehydration-kernel PR #122: docs: low-severity accuracy fixes + draft caveats

## Integration record

- **Status:** merged
- **Pull request:** [#122](https://github.com/underpass-ai/rehydration-kernel/pull/122)
- **Author:** [`tgarciai`](https://github.com/tgarciai)
- **Created:** `2026-06-02T21:32:18Z`
- **Merged:** `2026-06-02T23:09:37Z`
- **Base:** `main` at `5eabdfd0174e9fcba1981b43fbd362e74b4a612b`
- **Head:** `docs/low-severity-accuracy` at `50a0254df435254ddb4799cd87a68125166bb8cd`
- **Merge commit:** [`7f43eaea7960673bb94d6de7c2fcf8963d43c2ce`](https://github.com/underpass-ai/rehydration-kernel/commit/7f43eaea7960673bb94d6de7c2fcf8963d43c2ce)
- **Integration method:** merge commit
- **Audited tree range:** `ca927f6bb148a1c64d326139c34f79c20b2cbae5..7f43eaea7960673bb94d6de7c2fcf8963d43c2ce`

## Normalized impact

- **Classification:** `documentation`
- **Capability added:** No new capability is explicitly claimed by the PR title.
- **Behavior or maintenance changed:** docs: low-severity accuracy fixes + draft caveats
- **Capability removed:** No capability removal is explicitly claimed by the title or integrated commit subjects.

This classification is deliberately conservative. The title and integrated
commit subjects support the normalized claims; the complete PR description and
tree evidence below remain the authority for detailed behavior.

## Author's change description

## Summary

PR 4 (P2 — low-severity accuracy + caveats) of the kernel documentation audit follow-up (PR1 = #119, PR2 = #120, PR3 = #121).

Each candidate finding was adversarially verified against current code/docs: **11 confirmed, 6 refuted**. The refutations prevented 6 incorrect edits — `AGENT_SCOPES` env (does not exist in code), Neo4j client-cert framing (already precise everywhere), OTLP mTLS env vars (already documented), an `mtls` image tag (no such tag), a test-crate attribution (the test matrix has no crate column), and the `llm_reason_fabricated` predicate (the two doc forms are equivalent; the flagged one is actually more code-faithful).

## Changes

**README**
- Drop brittle counts: `17 active instruments` → `OTLP metric instruments` (actual ~30, spread across 4 crates); `270 unit tests` → `workspace unit tests` (actual ~591). Both numbers were stale and unmaintained.
- Add a caveat that the `:latest` quickstart image is non-reproducible; pin a `sha-<short-commit>` / `v*` tag or digest in production (the repo publishes those).

**docs/usage-guide.md**
- The projection runs **three** durable consumers (graph-node, graph-relation, node-detail), not two — fix the count, the "all three use explicit ack" line, and the NATS subjects table (`projection_stream.rs:8-13`).
- Add Further-Reading links to the KMP API design and the MCP stdio adapter, so the guide surfaces the `kernel_*` tool surface (previously absent).

**docs/product/kernel-context-api-design.md**
- Add a representative `page` object to the `near` response example — the prose promises page metadata and the proto carries `PageInfo` on every temporal RPC, but no example showed it.

**docs/operations/mcp-stdio.md**
- Qualify the Ingress TLS note: the default exposure profile forwards plaintext upstream h2c (`backend-protocol: GRPC`); GRPCS + backend mTLS is the non-default transport-security profile (`kubernetes-deploy.md`).

**docs/product/kernel-roadmap-milestones.md**
- `Date:` → `Last updated:` on a living planning doc (edited several times after the stated date).

**docs/research/**
- `PAPER_AGENTIC_CONTEXT_REHYDRATION.md`, `PAPER_SUBMISSION_DRAFT.md`: add the "draft / may lag implementation — verify against beta-status.md" caveat the catalog requires.
- `PLAN_GRAPH_EXPLORER.md`: drop two dead links (`BUG_DEPTH_TRAVERSAL.md`, `operations/graph-explorer-demo.md`) that exist nowhere in the repo.

## Verification
- 11/17 adversarially confirmed; 6 refuted (listed above).
- `near` example JSON re-validated (parses, `page` present).
- Pre-commit gate passed (clippy + workspace tests + coverage).

## Known, out of scope
- `PLAN_GRAPH_EXPLORER.md` still has ~20 pre-existing `../crates/...` links at the wrong relative depth (`../` vs `../../`). They are in a Historical/superseded doc and were not part of the verified G8-07 scope (which covered only the two truly-dead links), so they are intentionally left for a separate cleanup.

## Scope note
Final PR of the 4-PR audit follow-up. Independent of #119/#120/#121.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Verified repository evidence

- **Integrated commits:** 1
- **Changed files:** 8
- **Text additions/deletions:** +28 / -12
- **Binary files:** 0
- **Top-level areas:** `docs`, `README.md`

### Integrated commits

- [`50a0254df435`](https://github.com/underpass-ai/rehydration-kernel/commit/50a0254df435254ddb4799cd87a68125166bb8cd) docs: low-severity accuracy fixes + draft caveats

<details>
<summary>Changed paths (8)</summary>

- `M` `README.md` (+4 / -2)
- `M` `docs/operations/mcp-stdio.md` (+4 / -2)
- `M` `docs/product/kernel-context-api-design.md` (+7 / -1)
- `M` `docs/product/kernel-roadmap-milestones.md` (+1 / -1)
- `M` `docs/research/PAPER_AGENTIC_CONTEXT_REHYDRATION.md` (+2 / -0)
- `M` `docs/research/PAPER_SUBMISSION_DRAFT.md` (+2 / -0)
- `M` `docs/research/PLAN_GRAPH_EXPLORER.md` (+0 / -2)
- `M` `docs/usage-guide.md` (+8 / -4)

</details>

## Audit note

The repository diff above is calculated from the first parent of the recorded
merge commit to the merge commit itself. It therefore records what this PR
introduced into `main`, even when the branch was stale or GitHub used a squash
merge. Statements in the PR description are author claims; changed paths and
commits are repository evidence.
