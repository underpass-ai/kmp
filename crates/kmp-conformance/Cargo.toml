[package]
name = "kmp-conformance"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# Not on crates.io: in-repo gate, not a consumer contract.
publish = false

[lints]
workspace = true

[features]
default = []
# Runs the embedded arm against the SQLite engine as well (ADR-018).
sqlite = ["kmp-adapter-embedded/sqlite"]

[dependencies]
kmp-application.workspace = true
kmp-domain.workspace = true

[dev-dependencies]
kmp-adapter-embedded.workspace = true
kmp-testkit.workspace = true
tempfile = "3.26.0"
tokio.workspace = true
