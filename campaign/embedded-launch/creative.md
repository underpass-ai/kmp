# KMP Embedded — memory, with receipts

Three OBS-recorded MP4 masters. Each one makes a single bounded claim and
keeps a real PTY terminal beside the real ChronoLoom browser. Copy and focus
scenes must remain legible when the 1920 px canvas is distributed at 390 px.

1. **End the session. Keep the why.** A fresh KMP MCP process recovers a
   decision and evidence written by a process that exited.
2. **Process A writes it. Process B recovers the why.** Two local KMP MCP
   processes use one SQLite WAL store without export or import.
3. **Delete the wrong turn. Lose the why.** A deterministic product fixture
   retains a superseded hypothesis inside a four-hop audit path.

No master implies a real Codex or Claude session. Those names require a new raw
host capture. No master presents the deterministic incident fixture as customer
or production data.

Only master 1, **End the session. Keep the why.**, produces a GIF. The derivative is
`docs/assets/kmp-agent-loom.gif`; no second campaign GIF is allowed.
